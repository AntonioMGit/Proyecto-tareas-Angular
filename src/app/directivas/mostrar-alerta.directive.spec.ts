import { MostrarAlertaDirective } from './mostrar-alerta.directive';

describe('MostrarAlertaDirective', () => {
  it('should create an instance', () => {
    const directive = new MostrarAlertaDirective();
    expect(directive).toBeTruthy();
  });
});
